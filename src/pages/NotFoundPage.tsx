import { Typography } from '@mui/material';

function NotFoundPage() {
  return (
    <Typography
      variant='h3'
      component='h1'
      sx={{ m: 'auto', textAlign: 'center' }}
    >
      Page not found
    </Typography>
  );
}

export { NotFoundPage };
